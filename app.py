import requests
import streamlit as dtsj
from yt_playlist_duration import oaiwrnghj
import util.ajqaj as SX
dtsj.set_page_config(page_title="Youtube Playlist Duration Calculator", page_icon=":🏳️‍🌈:", layout="wide")
def load_lottieurl(url):
    r = requests.get(url)
    if r.status_code != 200:
        return None
    return r.json()
def local_css(x):
    with open(x) as f:
        dtsj.markdown(f"<style>{f.read()}</style>", unsafe_allow_html=True)
def youngsheckwes():
    dtsj.markdown(CALLIN, unsafe_allow_html=True)
def bitch(sdhr):
    fyj = str(sdhr.srhshr.a5eyh)
    ioh = str(sdhr.srhshr.awt4)
    ki = str(sdhr.srhshr.w34)
    ko = str(sdhr.aej5.a5eyh)
    ka = str(sdhr.aej5.awt4)
    lio = str(sdhr.aej5.w34)
    lem = sdhr.srhshr.detjh / 1.25
    ronaldo = sdhr.srhshr.detjh / 1.5
    cr7 = sdhr.srhshr.detjh / 1.75
    speeeeeeeeeee = sdhr.srhshr.detjh / 2
    naaaaaaaaaaaaaaw = \
        SX.wshre(lem)
    lolololo = \
        SX.wshre(ronaldo)
    tahja = \
        SX.wshre(cr7)
    tanya = \
        SX.wshre(speeeeeeeeeee)
    magy = str(sdhr.uj)
    dtsj.write(" ")
    bogba, notma, natynaty, hooooo, igothooooooooooooooo, oooooooooooooo, oooooooooooooooes = \
        dtsj.columns([0.9, 0.5, 0.5, 0.5, 1.5, 1.5, 1.5])
    with bogba:
        dtsj.write("Playlist Duration:")
        dtsj.write("No. of Videos:")
        dtsj.write("Average Video Duration:")
        dtsj.write("⠀")
        dtsj.write(f"Playlist Duration at 1.25x:")
        dtsj.write(f"Playlist Duration at 1.5x:")
        dtsj.write(f"Playlist Duration at 1.75x:")
        dtsj.write(f"Playlist Duration at 2x:")
    with notma:
        dtsj.write(f"Hours: {fyj}")
        dtsj.write(magy)
        dtsj.write(f"Hours: {ko}")
        dtsj.write("⠀")
        dtsj.write(f"Hours: {naaaaaaaaaaaaaaw.a5eyh}")
        dtsj.write(f"Hours: {lolololo.a5eyh}")
        dtsj.write(f"Hours: {tahja.a5eyh}")
        dtsj.write(f"Hours: {tanya.a5eyh}")
    with natynaty:
        dtsj.write(f"Minutes: {ioh}")
        dtsj.write("⠀")
        dtsj.write(f"Minutes: {ka}")
        dtsj.write("⠀")
        dtsj.write(f"Minutes: {naaaaaaaaaaaaaaw.awt4}")
        dtsj.write(f"Minutes: {lolololo.awt4}")
        dtsj.write(f"Minutes: {tahja.awt4}")
        dtsj.write(f"Minutes: {tanya.awt4}")
    with hooooo:
        dtsj.write(f"Seconds: {ki}")
        dtsj.write("⠀")
        dtsj.write(f"Seconds: {lio}")
        dtsj.write("⠀")
        dtsj.write(f"Seconds: {naaaaaaaaaaaaaaw.w34}")
        dtsj.write(f"Seconds: {lolololo.w34}")
        dtsj.write(f"Seconds: {tahja.w34}")
        dtsj.write(f"Seconds: {tanya.w34}")
    dtsj.write("##")
    dtsj.write("##")
local_css("style/style.css")
oaiwrnghj.__init__(oaiwrnghj, "AIzaSyADfggwBl8GReXvOJnNdImIXL52s8YUV9o")
CALLIN = """<span style='color: red;'>Playlist is invalid.</span>"""
with dtsj.container():
    dtsj.subheader("Hi, I am _CrystaLLL :wave:")
    dtsj.title("A Professional Dumbass.")
    dtsj.write(
        "Java go brrrrrrrr"
    )
    dtsj.write("[Learn More](https://www.merriam-webster.com/dictionary/dumbass)")
with dtsj.container():
    dtsj.write("---")
    ayoungniga = dtsj.columns(3)[1]
    with ayoungniga:
        dtsj.header("Youtube Playlist Duration")
with dtsj.container():
    phooofuck = dtsj.text_input("Playlist URL", placeholder="Playlist URL", label_visibility='hidden')
    if len(phooofuck) > 0:
        if phooofuck.__contains__("https://www.youtube.com/watch?v="):
            shit = oaiwrnghj.shr(oaiwrnghj, phooofuck)
            if shit.etjh:
                bitch(shit)
            else:
                youngsheckwes()
        else:
            youngsheckwes()
with dtsj.container():
    dtsj.write("---")
    dtsj.header("Get In Touch With Me!")
    igottired = """
    <form action="https://formsubmit.co/send.feedback.net@gmail.com" method="POST">
        <input type="hidden" name="_captcha" value="false">
        <input type="text" name="name" placeholder="Your name" required>
        <input type="email" name="email" placeholder="Your email" required>
        <textarea name="message" placeholder="Your message here" required></textarea>
        <button type="submit">Send</button>
    </form>
    """
    ah, aaaaaaaaah = dtsj.columns(2)
    with ah:
        dtsj.markdown(igottired, unsafe_allow_html=True)
    with aaaaaaaaah:
        dtsj.empty()
dtsj.write("B.S I know this is slow, but please, wait until it says 'Thank You'")
